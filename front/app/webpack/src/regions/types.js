/**
 * @typedef RegionItemType
 * @type {object}
 * @property {string} id
 * @property {string} img
 * @property {string} title
 * @property {string[]} xyhw
 *   the region expressed in absolute pixel values, as stringified numbers.
 *   NOTE: though the key is named `xyhw`, the coordinates are actually in xywh !!!
 * @property {string} canvas
 * @property {string} ref
 * @property {string} type
 */

export {}
